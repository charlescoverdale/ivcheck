% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iv_kitagawa.R
\name{iv_kitagawa}
\alias{iv_kitagawa}
\alias{iv_kitagawa.default}
\alias{iv_kitagawa.fixest}
\alias{iv_kitagawa.ivreg}
\title{Kitagawa (2015) test for instrument validity}
\usage{
iv_kitagawa(object, ...)

\method{iv_kitagawa}{default}(
  object,
  d,
  z,
  n_boot = 1000,
  alpha = 0.05,
  weighting = c("variance", "unweighted"),
  weights = NULL,
  parallel = TRUE,
  ...
)

\method{iv_kitagawa}{fixest}(
  object,
  n_boot = 1000,
  alpha = 0.05,
  weighting = c("variance", "unweighted"),
  weights = NULL,
  parallel = TRUE,
  ...
)

\method{iv_kitagawa}{ivreg}(
  object,
  n_boot = 1000,
  alpha = 0.05,
  weighting = c("variance", "unweighted"),
  weights = NULL,
  parallel = TRUE,
  ...
)
}
\arguments{
\item{object}{For the default method: a numeric outcome vector.
For the \code{fixest} and \code{ivreg} methods: a fitted instrumental variable
model from \link[fixest:feols]{fixest::feols} or \code{ivreg::ivreg()}.}

\item{...}{Further arguments passed to methods.}

\item{d}{Binary 0/1 treatment vector (default method only).}

\item{z}{Discrete instrument (numeric or factor, default method only).}

\item{n_boot}{Number of multiplier-bootstrap replications. Default 1000.}

\item{alpha}{Significance level for the returned verdict. Default 0.05.}

\item{weighting}{Test-statistic weighting. \code{"variance"} (default)
divides each pointwise difference by its plug-in standard error
estimator before taking the sup, as in Kitagawa (2015) section 4.
\code{"unweighted"} uses the raw positive-part KS of section 3. The
two are asymptotically equivalent at the boundary of the null;
\code{"variance"} has better finite-sample power when instrument cells
have unequal sizes.}

\item{weights}{Optional survey weights. Not yet implemented; reserved
for v0.2.0.}

\item{parallel}{Logical. Run bootstrap replications in parallel on
POSIX systems via \link[parallel:mclapply]{parallel::mclapply}. Default \code{TRUE}.}
}
\value{
An object of class \code{iv_test} with elements:
\item{test}{Character, always \code{"Kitagawa (2015)"}.}
\item{statistic}{Numeric test statistic (Kolmogorov-Smirnov
positive-part, scaled by sqrt(n)).}
\item{p_value}{Bootstrap p-value.}
\item{alpha}{Supplied significance level.}
\item{n_boot}{Number of bootstrap replications used.}
\item{boot_stats}{Numeric vector of bootstrap test statistics.}
\item{binding}{List identifying the binding \verb{(z, z', d, y)}
configuration of the observed statistic.}
\item{n}{Sample size.}
\item{call}{Matched call.}
}
\description{
Tests the joint implication of the local exclusion restriction and the
local monotonicity condition in a binary-treatment, discrete-instrument
setting. The null hypothesis is that the instrument is valid. Under the
null, the conditional distributions of the outcome given treatment
status, evaluated at each level of the instrument, must satisfy a set
of stochastic dominance inequalities. Rejection is evidence that at
least one of exclusion or monotonicity fails.
}
\details{
Let \verb{F(y, d | z) = Pr(Y <= y, D = d | Z = z)} denote the empirical
joint CDF of outcome and treatment conditional on the instrument.
Kitagawa (2015) shows that under IV validity there exists an ordering
of the instrument levels such that for every pair \verb{(z, z')} with \code{z}
ordered above \verb{z'}, and for every \code{y}, both
\verb{F(y, 1 | z') <= F(y, 1 | z)} and \verb{F(y, 0 | z) <= F(y, 0 | z')}. The
one-sided Kolmogorov-Smirnov statistic
\verb{T_n = sqrt(n) * max over (z, z', d, y) of [F(y, d | z) - F(y, d | z')]^+}
tests this implication in its most-violated direction. The
distribution of \code{T_n} is obtained by multiplier bootstrap with
Rademacher weights as in Kitagawa (2015) section 3.2.
}
\examples{
\donttest{
# Valid IV: compliers exist, no violations
set.seed(1)
n <- 500
z <- sample(0:1, n, replace = TRUE)
d <- rbinom(n, 1, 0.3 + 0.4 * z)
y <- rnorm(n, mean = d)
iv_kitagawa(y, d, z, n_boot = 200, parallel = FALSE)
}

}
\references{
Kitagawa, T. (2015). A Test for Instrument Validity. \emph{Econometrica},
83(5), 2043-2063. \doi{10.3982/ECTA11974}

Imbens, G. W. and Angrist, J. D. (1994). Identification and Estimation
of Local Average Treatment Effects. \emph{Econometrica}, 62(2), 467-475.
\doi{10.2307/2951620}
}
\seealso{
\code{\link[=iv_mw]{iv_mw()}} for the conditional version with covariates,
\code{\link[=iv_testjfe]{iv_testjfe()}} for the judge-design test, and \code{\link[=iv_check]{iv_check()}} for a
one-shot wrapper that runs all applicable tests.

Other iv_tests: 
\code{\link{iv_mw}()},
\code{\link{iv_testjfe}()}
}
\concept{iv_tests}
