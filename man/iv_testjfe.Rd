% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iv_testjfe.R
\name{iv_testjfe}
\alias{iv_testjfe}
\alias{iv_testjfe.default}
\alias{iv_testjfe.fixest}
\alias{iv_testjfe.ivreg}
\title{Frandsen-Lefgren-Leslie (2023) test for instrument validity in
judge-fixed-effects designs}
\usage{
iv_testjfe(object, ...)

\method{iv_testjfe}{default}(
  object,
  d,
  z,
  x = NULL,
  n_boot = 1000,
  alpha = 0.05,
  method = c("asymptotic", "bootstrap"),
  weights = NULL,
  parallel = TRUE,
  ...
)

\method{iv_testjfe}{fixest}(
  object,
  x = NULL,
  n_boot = 1000,
  alpha = 0.05,
  method = c("asymptotic", "bootstrap"),
  weights = NULL,
  parallel = TRUE,
  ...
)

\method{iv_testjfe}{ivreg}(
  object,
  x = NULL,
  n_boot = 1000,
  alpha = 0.05,
  method = c("asymptotic", "bootstrap"),
  weights = NULL,
  parallel = TRUE,
  ...
)
}
\arguments{
\item{object}{For the default method: a numeric outcome vector.
For the \code{fixest} and \code{ivreg} methods: a fitted instrumental variable
model from \link[fixest:feols]{fixest::feols} or \code{ivreg::ivreg()}.}

\item{...}{Further arguments passed to methods.}

\item{d}{Binary 0/1 treatment vector (default method only).}

\item{z}{Factor, integer, or matrix of mutually exclusive dummy
variables identifying the judge (or other random-assignment unit).}

\item{x}{Optional numeric vector, matrix, or data frame of covariates.
If supplied, \code{y} and \code{d} are residualised on \code{x} before the per-
judge means are computed.}

\item{n_boot}{Number of multiplier-bootstrap replications. Default 1000.}

\item{alpha}{Significance level for the returned verdict. Default 0.05.}

\item{method}{Reference distribution for the p-value. \code{"asymptotic"}
(default) uses the chi-squared with \code{K - 2} degrees of freedom.
\code{"bootstrap"} uses the multiplier bootstrap of the restricted-
model residual process. Asymptotic is fast and accurate for
moderate \code{K}; bootstrap is preferred for small \code{K} or if
errors are far from normal.}

\item{weights}{Optional survey weights. A non-negative numeric vector
of length equal to the sample size. Scaled internally so the mean
weight is 1.0 (preserving effective sample-size interpretation).
Applied to the empirical CDFs, the bootstrap multiplier process,
and the variance-weighted standard errors.}

\item{parallel}{Logical. Run bootstrap replications in parallel on
POSIX systems via \link[parallel:mclapply]{parallel::mclapply}. Default \code{TRUE}.}
}
\value{
An object of class \code{iv_test}; see \link{iv_kitagawa} for element
descriptions. Additional elements:
\item{n_judges}{Number of distinct judges / assignment groups.}
\item{coef}{Fitted weighted-LS slope and intercept of \code{mu_j} on \code{p_j}.}
\item{pairwise_late}{\verb{K x K} matrix of pairwise Wald LATE estimates
\code{(mu_j - mu_k) / (p_j - p_k)}. Under the null every entry
estimates the common complier LATE.}
\item{worst_pair}{List identifying the judge pair with the largest
deviation of its Wald LATE from the fitted slope; useful for
diagnosing the source of a rejection.}
}
\description{
Jointly tests the local exclusion and monotonicity assumptions when
the instruments are a set of mutually exclusive dummy variables (the
leniency-of-assigned-judge design). Supports binary and multivalued
discrete treatments. Under the joint null, the per-judge mean
outcome \verb{mu_j = E[Y | J = j]} must be a linear function of the
per-judge treatment propensities \verb{P(D = d | J = j)}. Rejection is
evidence that at least one of exclusion or monotonicity fails.
}
\details{
Under the joint null, each pair of judges \verb{(j, k)} identifies the
same complier LATE via the Wald estimator
\code{(mu_j - mu_k) / (p_j - p_k)}. The Frandsen-Lefgren-Leslie (2023)
test is the overidentification test of "all pairwise LATEs equal".
Under binary treatment with WLS weighting, that overidentification
test is algebraically the weighted sum of squared residuals from
the linear fit \code{mu_j = alpha + beta * p_j}, divided by a pooled
variance estimator. \code{iv_testjfe} computes this quadratic form and,
by default, compares to a chi-squared distribution with \code{K - 2}
degrees of freedom (the FLL asymptotic form). The multiplier
bootstrap of the restricted residual process is available via
\code{method = "bootstrap"} for small-K robustness.

The returned object includes \code{pairwise_late}, the \verb{K x K} matrix of
pairwise Wald LATE estimates, and \code{worst_pair}, the judge pair with
the largest absolute deviation from the fitted slope. These are
diagnostic outputs in the sense of the paper's Figure 2: a pair
whose Wald LATE deviates far from the common slope is the first
place to look when investigating a rejection.

Multivalued treatment is supported: for \code{D} with \code{M + 1} distinct
values (\verb{0, 1, ..., M}), the fit becomes a multiple WLS regression
of \code{mu_j} on the \code{M}-vector \verb{(P(D = 1 | J), ..., P(D = M | J))} and
the test statistic is compared to \verb{chi^2_\{K - M - 1\}} (FLL 2023
section 4). \code{pairwise_late} and \code{worst_pair} are only defined for
binary \code{D} and return \code{NULL} otherwise.
}
\examples{
\donttest{
set.seed(1)
n <- 2000
judge <- sample.int(20, n, replace = TRUE)
d <- rbinom(n, 1, 0.3 + 0.02 * judge)
y <- rnorm(n, mean = d)
iv_testjfe(y, d, judge, n_boot = 200, parallel = FALSE)
}

}
\references{
Frandsen, B. R., Lefgren, L. J., and Leslie, E. C. (2023). Judging
Judge Fixed Effects. \emph{American Economic Review}, 113(1), 253-277.
\doi{10.1257/aer.20201860}

Imbens, G. W. and Angrist, J. D. (1994). Identification and Estimation
of Local Average Treatment Effects. \emph{Econometrica}, 62(2), 467-475.
\doi{10.2307/2951620}
}
\seealso{
\code{\link[=iv_kitagawa]{iv_kitagawa()}} for the unconditional binary-treatment test,
\code{\link[=iv_mw]{iv_mw()}} for the conditional version with covariates, and
\code{\link[=iv_check]{iv_check()}} for a one-shot wrapper that runs all applicable tests.

Other iv_tests: 
\code{\link{iv_kitagawa}()},
\code{\link{iv_mw}()}
}
\concept{iv_tests}
