% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iv_hm.R
\name{iv_hm}
\alias{iv_hm}
\alias{iv_hm.default}
\alias{iv_hm.fixest}
\alias{iv_hm.ivreg}
\title{Huber-Mellace (2015) test for instrument validity}
\usage{
iv_hm(object, ...)

\method{iv_hm}{default}(
  object,
  d,
  z,
  n_boot = 1000,
  alpha = 0.05,
  weights = NULL,
  parallel = TRUE,
  ...
)

\method{iv_hm}{fixest}(
  object,
  n_boot = 1000,
  alpha = 0.05,
  weights = NULL,
  parallel = TRUE,
  ...
)

\method{iv_hm}{ivreg}(
  object,
  n_boot = 1000,
  alpha = 0.05,
  weights = NULL,
  parallel = TRUE,
  ...
)
}
\arguments{
\item{object}{For the default method: a numeric outcome vector.
For the \code{fixest} and \code{ivreg} methods: a fitted instrumental variable
model from \link[fixest:feols]{fixest::feols} or \code{ivreg::ivreg()}.}

\item{...}{Further arguments passed to methods.}

\item{d}{Binary 0/1 treatment vector (default method only).}

\item{z}{Discrete instrument (numeric or factor, default method only).}

\item{n_boot}{Number of multiplier-bootstrap replications.}

\item{alpha}{Significance level.}

\item{weights}{Optional non-negative numeric weights. Applied to the
stratum-conditional means and to the multiplier bootstrap.}

\item{parallel}{Logical. Run bootstrap in parallel on POSIX.}
}
\value{
An object of class \code{iv_test} with elements:
\item{test}{Character, \code{"Huber-Mellace (2015)"}.}
\item{statistic}{Numeric test statistic, the max sqrt(n)-scaled
positive-part violation of the implied-complier-mean bounds.}
\item{p_value}{Bootstrap p-value.}
\item{alpha}{Significance level used.}
\item{implied_means}{Implied complier mean outcomes for \code{D = 0}
and \code{D = 1}, with their bound-based admissibility range.}
\item{boot_stats}{Vector of bootstrap test statistics.}
\item{n}{Sample size.}
}
\description{
Tests the local exclusion and monotonicity assumptions via mean-based
moment inequalities on the implied complier outcome distribution.
Under the joint null, the principal-strata decomposition of the
observed conditional mean outcomes yields an implied mean outcome
for compliers that must lie inside the support of the outcome
variable. Rejection is evidence that at least one of exclusion or
monotonicity fails.
}
\details{
Huber and Mellace (2015) derive four inequality constraints on the
mean outcome of compliers that must hold under exclusion and
monotonicity. For binary treatment and binary instrument with
instrument levels ordered by first-stage propensity:
\deqn{E[Y \mid D = 1, Z = \mathrm{high}] \in
  \Bigl[ \frac{P_{AT}}{P_{AT}+P_{C}} \, y_{\min}
         + \frac{P_{C}}{P_{AT}+P_{C}} \, E[Y \mid D=1, Z=\mathrm{low}],
         \;
         \frac{P_{AT}}{P_{AT}+P_{C}} \, y_{\max}
         + \frac{P_{C}}{P_{AT}+P_{C}} \, E[Y \mid D=1, Z=\mathrm{low}]
  \Bigr],}
and analogously for the \code{D = 0} cells. The test statistic is the
maximum sqrt(n)-scaled positive-part violation across the four
inequalities. Critical values come from a multiplier bootstrap.

\code{iv_hm} is complementary to \code{iv_kitagawa}. Kitagawa's distributional
inequalities on joint CDFs and Huber-Mellace's mean-based bounds
target different violations: Kitagawa has higher power against
shape-level failures; Huber-Mellace has higher power against
location-level failures and against violations that shift implied
complier means outside the observed outcome support.

The package also implements related tests: \code{iv_kitagawa} for the CDF
inequalities, \code{iv_mw} for the covariate extension via intersection
bounds, and \code{iv_testjfe} for the judge-design case.
}
\examples{
\donttest{
set.seed(1)
n <- 500
z <- sample(0:1, n, replace = TRUE)
d <- rbinom(n, 1, 0.3 + 0.4 * z)
y <- rnorm(n, mean = d)
iv_hm(y, d, z, n_boot = 200, parallel = FALSE)
}

}
\references{
Huber, M. and Mellace, G. (2015). Testing Instrument Validity for
LATE Identification Based on Inequality Moment Constraints.
\emph{Review of Economics and Statistics}, 97(2), 398-411.
\doi{10.1162/REST_a_00450}

Imbens, G. W. and Angrist, J. D. (1994). Identification and Estimation
of Local Average Treatment Effects. \emph{Econometrica}, 62(2), 467-475.
\doi{10.2307/2951620}
}
\seealso{
\code{\link[=iv_kitagawa]{iv_kitagawa()}} for the CDF-based form, \code{\link[=iv_mw]{iv_mw()}} for the
conditional (covariate) extension, \code{\link[=iv_testjfe]{iv_testjfe()}} for judge designs,
and \code{\link[=iv_check]{iv_check()}} for a wrapper that runs all applicable tests.

Other iv_tests: 
\code{\link{iv_kitagawa}()},
\code{\link{iv_mw}()},
\code{\link{iv_testjfe}()}
}
\concept{iv_tests}
